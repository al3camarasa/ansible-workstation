#!/usr/bin/env xdg-open
[Desktop Entry]
Version=1.0
Type=Application
Terminal=false
Exec={{ app.path }}{% if app.disable_sandbox | default(false) %} --no-sandbox{% endif %}

Name={{ app.comment }}
Comment={{ app.comment }}
Icon={{ app.icon_path }}